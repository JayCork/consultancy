import styles from "./EvidenceReview.module.css";

interface EvidenceReviewProps {
  // Define your props here
}

export const EvidenceReview = (props: EvidenceReviewProps) => {
  return (
    <div class={styles.base}>
      EvidenceReview page
    </div>
  );
};
